<section
  id="architecture"
  class="architecture-section"
  aria-labelledby="architecture-title"
>
  <div class="architecture-heading">
    <div>
      <p class="architecture-eyebrow">Platform architecture</p>

      <h2 id="architecture-title">
        Designed as a connected experience system.
      </h2>
    </div>

    <p class="architecture-introduction">
      Create Aether separates presentation, runtime behaviour, modular
      services and future platform infrastructure so each layer can evolve
      without tightly coupling the entire system.
    </p>
  </div>

  <div class="architecture-layout">
    <div class="architecture-diagram">
      <div class="architecture-legend" aria-label="Architecture status">
        <span>
          <i class="legend-dot current"></i>
          Current
        </span>

        <span>
          <i class="legend-dot prototype"></i>
          Prototype
        </span>

        <span>
          <i class="legend-dot planned"></i>
          Planned
        </span>
      </div>

      <ol class="architecture-flow">
        <li class="architecture-node current">
          <div class="node-icon" aria-hidden="true">
            <svg viewBox="0 0 48 48">
              <rect x="7" y="9" width="34" height="26" rx="4"></rect>
              <path d="M17 41h14"></path>
              <path d="M24 35v6"></path>
            </svg>
          </div>

          <div>
            <span class="node-status">Current</span>
            <h3>Browser</h3>
            <p>The visitor interacts with the live experience.</p>
          </div>
        </li>

        <li class="flow-connector" aria-hidden="true">
          <span></span>
        </li>

        <li class="architecture-node current">
          <div class="node-icon" aria-hidden="true">
            <svg viewBox="0 0 48 48">
              <path d="M8 13h32v24H8z"></path>
              <path d="M8 20h32"></path>
              <circle cx="13" cy="16.5" r="1"></circle>
              <circle cx="17" cy="16.5" r="1"></circle>
            </svg>
          </div>

          <div>
            <span class="node-status">Current</span>
            <h3>Astro Interface</h3>
            <p>Fast static delivery, accessible markup and component UI.</p>
          </div>
        </li>

        <li class="flow-connector" aria-hidden="true">
          <span></span>
        </li>

        <li class="architecture-node current">
          <div class="node-icon" aria-hidden="true">
            <svg viewBox="0 0 48 48">
              <path d="M24 8 39 17 24 26 9 17 24 8Z"></path>
              <path d="m11 25 13 8 13-8"></path>
              <path d="m14 33 10 6 10-6"></path>
            </svg>
          </div>

          <div>
            <span class="node-status">Current</span>
            <h3>Interactive Demo</h3>
            <p>Experience state, visual themes and visitor preferences.</p>
          </div>
        </li>

        <li class="flow-connector" aria-hidden="true">
          <span></span>
        </li>

        <li class="architecture-node prototype">
          <div class="node-icon" aria-hidden="true">
            <svg viewBox="0 0 48 48">
              <rect x="9" y="9" width="30" height="30" rx="7"></rect>
              <circle cx="24" cy="24" r="6"></circle>
              <path d="M24 14v4"></path>
              <path d="M24 30v4"></path>
              <path d="M14 24h4"></path>
              <path d="M30 24h4"></path>
            </svg>
          </div>

          <div>
            <span class="node-status">Prototype</span>
            <h3>Aether Runtime</h3>
            <p>Coordinates lifecycle, configuration and experience state.</p>
          </div>
        </li>

        <li class="flow-connector" aria-hidden="true">
          <span></span>
        </li>

        <li class="architecture-node prototype">
          <div class="node-icon" aria-hidden="true">
            <svg viewBox="0 0 48 48">
              <circle cx="13" cy="24" r="4"></circle>
              <circle cx="35" cy="13" r="4"></circle>
              <circle cx="35" cy="35" r="4"></circle>
              <path d="m17 22 14-7"></path>
              <path d="m17 26 14 7"></path>
            </svg>
          </div>

          <div>
            <span class="node-status">Prototype</span>
            <h3>Events and Services</h3>
            <p>Decoupled communication between reusable experience modules.</p>
          </div>
        </li>

        <li class="flow-connector planned" aria-hidden="true">
          <span></span>
        </li>

        <li class="architecture-node planned">
          <div class="node-icon" aria-hidden="true">
            <svg viewBox="0 0 48 48">
              <path d="m18 15-9 9 9 9"></path>
              <path d="m30 15 9 9-9 9"></path>
              <path d="m27 10-6 28"></path>
            </svg>
          </div>

          <div>
            <span class="node-status">Planned</span>
            <h3>FastAPI Platform API</h3>
            <p>Experience configuration, accounts and application services.</p>
          </div>
        </li>

        <li class="flow-connector planned" aria-hidden="true">
          <span></span>
        </li>

        <li class="architecture-node planned">
          <div class="node-icon" aria-hidden="true">
            <svg viewBox="0 0 48 48">
              <ellipse cx="24" cy="13" rx="14" ry="6"></ellipse>
              <path d="M10 13v11c0 3.3 6.3 6 14 6s14-2.7 14-6V13"></path>
              <path d="M10 24v11c0 3.3 6.3 6 14 6s14-2.7 14-6V24"></path>
            </svg>
          </div>

          <div>
            <span class="node-status">Planned</span>
            <h3>PostgreSQL</h3>
            <p>Saved presets, users, versions and shareable experiences.</p>
          </div>
        </li>
      </ol>
    </div>

    <aside class="developer-preview">
      <div class="code-window">
        <div class="code-toolbar">
          <div class="window-controls" aria-hidden="true">
            <span></span>
            <span></span>
            <span></span>
          </div>

          <span>experience.ts</span>
        </div>

        <div class="code-heading">
          <span>Target developer API</span>
          <strong>Concept preview</strong>
        </div>

        <pre aria-label="Conceptual Aether API example"><code><span class="code-comment">// Proposed Create Aether API</span>
<span class="code-keyword">import</span> &#123; Aether &#125; <span class="code-keyword">from</span>
  <span class="code-string">'@createaether/core'</span>;

<span class="code-keyword">const</span> experience =
  Aether.<span class="code-function">createExperience</span>(&#123;
    preset: <span class="code-string">'ocean'</span>,
    particles: <span class="code-value">true</span>,
    ambience: <span class="code-value">true</span>,
    intensity: <span class="code-number">0.65</span>,
  &#125;);

experience.<span class="code-function">mount</span>(
  <span class="code-string">'#aether-stage'</span>
);</code></pre>

        <div class="code-footer">
          <span class="code-pulse" aria-hidden="true"></span>
          API design in development
        </div>
      </div>

      <div class="architecture-note">
        <span class="note-icon" aria-hidden="true">◇</span>

        <div>
          <h3>Built to evolve in layers</h3>
          <p>
            The current Astro experience remains lightweight while future
            backend services can be introduced without replacing the public
            interface.
          </p>
        </div>
      </div>
    </aside>
  </div>
</section>

<style>
  .architecture-section {
    padding: 130px 0;
    border-bottom: 1px solid rgba(255, 255, 255, 0.1);
  }

  .architecture-heading {
    margin-bottom: 72px;
    display: grid;
    grid-template-columns: minmax(0, 1.2fr) minmax(280px, 0.8fr);
    align-items: end;
    gap: clamp(44px, 7vw, 100px);
  }

  .architecture-eyebrow {
    margin: 0 0 22px;
    color: #66e3ff;
    font-size: 0.78rem;
    font-weight: 700;
    letter-spacing: 0.16em;
    text-transform: uppercase;
  }

  .architecture-heading h2 {
    max-width: 760px;
    margin: 0;
    font-size: clamp(2.3rem, 5vw, 4.5rem);
    line-height: 1.05;
    letter-spacing: -0.045em;
  }

  .architecture-introduction {
    margin: 0;
    color: #9ba1ad;
    font-size: 1.02rem;
    line-height: 1.8;
  }

  .architecture-layout {
    display: grid;
    grid-template-columns: minmax(0, 1.08fr) minmax(340px, 0.92fr);
    align-items: start;
    gap: clamp(30px, 5vw, 72px);
  }

  .architecture-diagram,
  .code-window,
  .architecture-note {
    border: 1px solid rgba(255, 255, 255, 0.09);
    background:
      linear-gradient(
        145deg,
        rgba(255, 255, 255, 0.035),
        rgba(255, 255, 255, 0.012)
      );
  }

  .architecture-diagram {
    padding: 26px;
    border-radius: 24px;
    box-shadow:
      inset 0 1px 0 rgba(255, 255, 255, 0.04),
      0 30px 80px rgba(0, 0, 0, 0.2);
  }

  .architecture-legend {
    margin-bottom: 30px;
    padding-bottom: 20px;
    display: flex;
    flex-wrap: wrap;
    gap: 18px;
    border-bottom: 1px solid rgba(255, 255, 255, 0.07);
    color: #8d97aa;
    font-size: 0.68rem;
  }

  .architecture-legend span {
    display: inline-flex;
    align-items: center;
    gap: 7px;
  }

  .legend-dot {
    width: 7px;
    height: 7px;
    border-radius: 50%;
  }

  .legend-dot.current {
    background: #66e3ff;
    box-shadow: 0 0 10px rgba(102, 227, 255, 0.8);
  }

  .legend-dot.prototype {
    background: #9e91ff;
    box-shadow: 0 0 10px rgba(158, 145, 255, 0.65);
  }

  .legend-dot.planned {
    border: 1px solid #737d90;
    background: transparent;
  }

  .architecture-flow {
    margin: 0;
    padding: 0;
    display: grid;
    list-style: none;
  }

  .architecture-node {
    position: relative;
    min-height: 116px;
    padding: 20px;
    display: grid;
    grid-template-columns: 54px minmax(0, 1fr);
    align-items: center;
    gap: 18px;
    border: 1px solid rgba(255, 255, 255, 0.075);
    border-radius: 17px;
    background: rgba(255, 255, 255, 0.022);
    transition:
      transform 200ms ease,
      border-color 200ms ease,
      background 200ms ease;
  }

  .architecture-node:hover {
    transform: translateX(5px);
    border-color: rgba(102, 227, 255, 0.25);
    background: rgba(102, 227, 255, 0.035);
  }

  .architecture-node.prototype {
    border-color: rgba(158, 145, 255, 0.14);
  }

  .architecture-node.planned {
    border-style: dashed;
    opacity: 0.76;
  }

  .node-icon {
    width: 54px;
    height: 54px;
    display: grid;
    place-items: center;
    border: 1px solid rgba(102, 227, 255, 0.18);
    border-radius: 14px;
    color: #66e3ff;
    background: rgba(102, 227, 255, 0.045);
  }

  .prototype .node-icon {
    border-color: rgba(158, 145, 255, 0.2);
    color: #9e91ff;
    background: rgba(158, 145, 255, 0.045);
  }

  .planned .node-icon {
    border-color: rgba(139, 148, 166, 0.15);
    color: #7f899c;
    background: rgba(255, 255, 255, 0.02);
  }

  .node-icon svg {
    width: 27px;
    height: 27px;
    fill: none;
    stroke: currentColor;
    stroke-width: 1.6;
    stroke-linecap: round;
    stroke-linejoin: round;
  }

  .node-status {
    display: block;
    margin-bottom: 7px;
    color: #66e3ff;
    font-size: 0.61rem;
    font-weight: 700;
    letter-spacing: 0.12em;
    text-transform: uppercase;
  }

  .prototype .node-status {
    color: #9e91ff;
  }

  .planned .node-status {
    color: #7f899c;
  }

  .architecture-node h3 {
    margin: 0 0 6px;
    color: #f2efe9;
    font-size: 1rem;
  }

  .architecture-node p {
    margin: 0;
    color: #8e97a8;
    font-size: 0.78rem;
    line-height: 1.55;
  }

  .flow-connector {
    position: relative;
    height: 34px;
    display: grid;
    place-items: center;
  }

  .flow-connector::before {
    width: 1px;
    height: 100%;
    content: "";
    background:
      linear-gradient(
        to bottom,
        rgba(102, 227, 255, 0.16),
        #66e3ff,
        rgba(102, 227, 255, 0.16)
      );
  }

  .flow-connector span {
    position: absolute;
    width: 5px;
    height: 5px;
    border-radius: 50%;
    background: #66e3ff;
    box-shadow: 0 0 10px #66e3ff;
    animation: architecture-signal 2.4s ease-in-out infinite;
  }

  .flow-connector.planned::before {
    background:
      repeating-linear-gradient(
        to bottom,
        #596274 0,
        #596274 4px,
        transparent 4px,
        transparent 8px
      );
  }

  .flow-connector.planned span {
    display: none;
  }

  .developer-preview {
    position: sticky;
    top: 28px;
    display: grid;
    gap: 20px;
  }

  .code-window {
    overflow: hidden;
    border-radius: 24px;
    box-shadow:
      inset 0 1px 0 rgba(255, 255, 255, 0.04),
      0 30px 80px rgba(0, 0, 0, 0.24);
  }

  .code-toolbar {
    min-height: 54px;
    padding: 0 18px;
    display: flex;
    align-items: center;
    justify-content: space-between;
    border-bottom: 1px solid rgba(255, 255, 255, 0.07);
    color: #788397;
    font-family: ui-monospace, SFMono-Regular, Consolas, monospace;
    font-size: 0.68rem;
  }

  .window-controls {
    display: flex;
    gap: 6px;
  }

  .window-controls span {
    width: 8px;
    height: 8px;
    border-radius: 50%;
    background: #465064;
  }

  .window-controls span:nth-child(2) {
    background: #596789;
  }

  .window-controls span:nth-child(3) {
    background: #49798c;
  }

  .code-heading {
    padding: 24px 24px 0;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 16px;
  }

  .code-heading span {
    color: #dce2ec;
    font-size: 0.82rem;
    font-weight: 650;
  }

  .code-heading strong {
    padding: 5px 8px;
    border: 1px solid rgba(158, 145, 255, 0.18);
    border-radius: 999px;
    color: #9e91ff;
    background: rgba(158, 145, 255, 0.045);
    font-size: 0.58rem;
    font-weight: 650;
  }

  pre {
    margin: 0;
    padding: 28px 24px 32px;
    overflow-x: auto;
    color: #cbd3df;
    font-family: ui-monospace, SFMono-Regular, Consolas, monospace;
    font-size: clamp(0.72rem, 1.4vw, 0.82rem);
    line-height: 1.8;
    tab-size: 2;
  }

  .code-comment {
    color: #647188;
  }

  .code-keyword {
    color: #9e91ff;
  }

  .code-string {
    color: #68d9b0;
  }

  .code-function {
    color: #66e3ff;
  }

  .code-value {
    color: #f0b87a;
  }

  .code-number {
    color: #80afff;
  }

  .code-footer {
    min-height: 48px;
    padding: 0 24px;
    display: flex;
    align-items: center;
    gap: 9px;
    border-top: 1px solid rgba(255, 255, 255, 0.065);
    color: #778297;
    font-size: 0.65rem;
  }

  .code-pulse {
    width: 6px;
    height: 6px;
    border-radius: 50%;
    background: #9e91ff;
    box-shadow: 0 0 10px #9e91ff;
    animation: code-status-pulse 2.2s ease-in-out infinite;
  }

  .architecture-note {
    padding: 22px;
    display: grid;
    grid-template-columns: 38px minmax(0, 1fr);
    gap: 15px;
    border-radius: 18px;
  }

  .note-icon {
    width: 38px;
    height: 38px;
    display: grid;
    place-items: center;
    border: 1px solid rgba(102, 227, 255, 0.18);
    border-radius: 11px;
    color: #66e3ff;
    background: rgba(102, 227, 255, 0.04);
  }

  .architecture-note h3 {
    margin: 1px 0 7px;
    color: #e9e6e0;
    font-size: 0.86rem;
  }

  .architecture-note p {
    margin: 0;
    color: #858fa1;
    font-size: 0.72rem;
    line-height: 1.65;
  }

  @keyframes architecture-signal {
    0%,
    100% {
      opacity: 0.2;
      transform: translateY(-10px);
    }

    50% {
      opacity: 1;
      transform: translateY(10px);
    }
  }

  @keyframes code-status-pulse {
    0%,
    100% {
      opacity: 0.4;
    }

    50% {
      opacity: 1;
    }
  }

  @media (max-width: 980px) {
    .architecture-heading,
    .architecture-layout {
      grid-template-columns: 1fr;
    }

    .architecture-heading {
      gap: 32px;
    }

    .developer-preview {
      position: static;
    }
  }

  @media (max-width: 560px) {
    .architecture-section {
      padding: 90px 0;
    }

    .architecture-heading {
      margin-bottom: 46px;
    }

    .architecture-diagram {
      padding: 17px;
    }

    .architecture-node {
      min-height: 108px;
      padding: 16px;
      grid-template-columns: 46px minmax(0, 1fr);
      gap: 14px;
    }

    .node-icon {
      width: 46px;
      height: 46px;
    }

    .architecture-legend {
      gap: 12px;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .architecture-node,
    .flow-connector span,
    .code-pulse {
      animation: none;
      transition: none;
    }

    .architecture-node:hover {
      transform: none;
    }
  }
</style>
